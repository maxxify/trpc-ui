{
  "devDependencies": {
    "@biomejs/biome": "2.5.0",
    "@tsconfig/docusaurus": "^1.0.7",
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  },
  "license": "MIT",
  "name": "trpc-ui-monorepo",
  "packageManager": "pnpm@11.5.0",
  "private": true,
  "scripts": {
    "biome:check": "biome check --max-diagnostics=none",
    "biome:check:changed": "biome check --changed --max-diagnostics=none",
    "biome:check:errors": "biome check --max-diagnostics=none --diagnostic-level=error",
    "biome:check:fix": "biome check --write --unsafe --max-diagnostics=none",
    "build": "pnpm -F trpc-parser build && pnpm -F trpc-react build && pnpm -F trpc-ui build",
    "build:dev-app": "pnpm -F dev-app build",
    "build:parser": "pnpm -F trpc-parser build",
    "build:react": "pnpm -F trpc-react build",
    "build:test-app": "pnpm -F test-app build",
    "build:ui": "pnpm -F trpc-ui build",
    "cpy:readme": "cp README.MD packages/trpc-ui/README.md",
    "dev:dev-app": "pnpm -F dev-app dev",
    "dev:test-app": "pnpm -F test-app dev",
    "start:test-app": "pnpm -F test-app start",
    "test": "pnpm -F trpc-parser test"
  },
  "version": "0.0.0"
}
